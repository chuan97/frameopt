import: models.cg_pramp:CGPRampModel
init:
  maxiter: 15000
  seed: 42
  step: 20
  scheduler:
    import: frameopt.model.p_scheduler:AdaptivePScheduler
    init:
      p0: 4.0
      p_max: 1.0e+9
      window: 5