import: models.tr_pramp:TRPRampModel
init:
  maxiter: 30000
  seed: 42
  step: 20
  restarts: 20
  scheduler:
    import: frameopt.model.p_scheduler:AdaptivePScheduler
    init:
      p0: 4.0
      p_max: 1.0e+12
      window: 5
  energy:
    import: frameopt.core.energy:mellowmax_coherence
  grad:
    import: frameopt.core.energy:grad_mellowmax_coherence